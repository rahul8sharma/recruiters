function selectTemplate(selectTag) {
  var body = $(selectTag).find(":selected").attr("body");
  var subject = $(selectTag).find(":selected").attr("subject");
  var from = $(selectTag).find(":selected").attr("from");
  var template_name = $(selectTag).find(":selected").attr("template_name");
  if(body) {
    $(".default-email").hide();
    $(".custom-email").show();
    $(".email-body").html("<i>"+body+"</i>");
    $(".email-subject").html("<i>"+subject+"</i>");
    $(".email-from").html("<i>"+from+"</i>");
    $('input[name="template_name"]').attr("value", template_name);
    $('#template_name').html("E-mail Template: "+template_name);
  } else {
    $(".custom-email").hide();
    $(".default-email").show();
  }
}

$("#edit_assessment").on("submit", function(){
  if (confirm($('#edit_assessment').attr('message'))){
    return true;
  }else{
    return false;
  }
});
