Hi,
<p>
  Exit Survey Report is ready for  <%=@report_hash[:candidate][:name] %>
</p>
<p>
  Report URL:
  <%= s3_report_company_exit_survey_url(:company_id => @report_hash[:company_id], :id => @report_hash[:survey_id],:candidate_id => @report_hash[:candidate_id], :report_id => @report_hash[:report_id], :view_mode => "html")
    %>
</p>
<p>
  You may alternatively view the report by copy-pasting the link in your web browser's address bar and visiting the link or by logging into your Jombay Account.
</p>
<%= render :partial => 'shared/mailer/contact' %>
<%= render :partial => 'shared/mailer/regards' %>
<%= render :partial => 'shared/mailer/footer' %>
