.badge_label.clearfix.span9.first
  .badge_count
    #{ job_category.count }
  .badge_content
    #{ job_category.name }
  %a.close{"data-dismiss" => "alert", :href => "[data-id='#{ job_category.id }']"} ×
  = hidden_field_tag("basic[job_preferences][preferred_job_categories][query_options][id][]", job_category.id)