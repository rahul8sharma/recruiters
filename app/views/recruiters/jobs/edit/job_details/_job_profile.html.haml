.badge_label.clearfix.span9.first
  -#
    .badge_count
      #{ job_category.count }
  .badge_content
    #{ truncate(job_profile.name, length: 50) }
  %a.close{"data-dismiss" => "alert", :href => "[data-id='#{ job_profile.id }']"} ×
  = hidden_field_tag("basic[job_preferences][preferred_job_profiles][query_options][id][]", job_profile.id)