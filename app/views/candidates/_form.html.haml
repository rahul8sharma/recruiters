= form_tag import_company_assessment_candidates_path(:company_id => params[:company_id], :assessment_id => params[:assessment_id]), multipart: true, :method => :post do
  = file_field_tag :file
  .actions
    = submit_tag 'Upload Candidates'
