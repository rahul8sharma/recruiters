- if @report.report_data[:enable_score_scale_section]  
  .score_scale
    .container
      .divider2
      .white.huge-text
        Jombay Score Scale
      .divider2
      .text-center
        = image_tag("#{Rails.application.config.action_controller.asset_host}/#{asset_path("report/html/score_scale.png")}")
      .divider2
