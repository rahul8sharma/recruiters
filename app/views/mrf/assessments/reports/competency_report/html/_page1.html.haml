#overview.section01
  .overall_report
    .container
      .divider6
      .user-name.pull-left
        = @report.report_data[:user][:name]
      .final-score.pull-right
        #{get_assessment_purpose(@report)}&#176; Profiling Report
      .clr
      .extra-medium-text.pull-left
        #{get_assessment_purpose(@report)}&#176; Profiling: 
        = @report.report_data[:assessment][:name]
        .divider1
      .clr
      .medium-text.pull-left
        Exercise Completed On:
        = hdate(@report.report_data[:generated_at])
      .clr
    - section_value.each do |value|
      = render :partial => value["path"]