#traits.traits_under_competencies.scores_competencies_graph.section02.individual 
  .divider2
  .container
    .title.pull-left
      #{get_assessment_purpose(@report)}&#176; and Self Ratings on Traits
    - section_value.each do |section|
      = render :partial => section["path"], :locals => { :section_value => section["children"]}
