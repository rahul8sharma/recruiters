.span11.margin-auto
  = form_for(@company, as: :company, url: sign_up_path, method: :post, html: { class: "" }) do |f|
    = f.fields_for :admin do |af|  
      .control-group
        = af.text_field :name, class: "fw", placeholder: "Full Name", required: true
      .control-group
        = af.text_field :email, placeholder: "Company Email", class: "fw pull-left", required: true
        .info-tooltip-control.pull-left
          .info-tooltip.gray-darker
            We encourage you to enter your company email address (not personal email address) for getting full access & privileges. Personal email address is only recommended if your company blocks external emails to your company email account.
            .left-caret
        .clr
      .control-group
        = af.password_field :password, placeholder: "Enter a password of your choice", class: "fw", required: true
      .control-group
        = af.text_field :mobile, placeholder: "Contact Number", class: "fw pull-left", required: true
        .info-tooltip-control.pull-left
          .info-tooltip.gray-darker
            Required to ensure that nobody misused your identity. Don't worry all information you provide is completely confidential
            .left-caret
        .clr
    .control-group
      = f.text_field :name, title: "Sign to Jombay", placeholder: "Company Name", class: "fw", required: true
    .control-group
      = f.select :country, @countries.map(&:name), { prompt: "Country" }, { class: "fw", required: true }
    %button.btn.btn-warning.large-text.fw{:id => "sign-up"}
      Sign Up
    .divider1
    .text-center.default-text
      By Clicking “Sign Up” above you agree to the 
      %a.underlined{:href => "/terms-and-conditions/", :target=>"_blank", :title => "Jombay Terms and Conditions"}
        Terms and Conditions