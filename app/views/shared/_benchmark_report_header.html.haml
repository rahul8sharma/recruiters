- if @view_mode != "html"
  .header.clearfix
    .logo
      - if @view_mode == "html"
        = image_tag ('report/jombay-logo-icon.png')
      - else
        = wicked_pdf_image_tag ('report/jombay-logo-icon.png')
    .left-side
    .right-side
      .pull-left
        Assessment Name: 
      .pull-left
        = @assessment.name
      .clr

