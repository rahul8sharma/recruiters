.content-sub-nav.clearfix
  %a.menu{class: (["statistics"].include?(params[:action]) ? "active" : ""), href: user_statistics_company_mrf_assessment_path(@company.id, @assessment.id, @user.id)}
    Statistics
  %a.menu{class: (["stakeholders"].include?(params[:action]) ? "active" : ""), href: user_stakeholders_company_mrf_assessment_path(@company.id, @assessment.id, @user.id)}
    Stakeholders

