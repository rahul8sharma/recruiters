.give-feedback
  .give-feedback-inner
    .main-heading{ title: @report.report_hash[:user][:name] }
      Give Report Feedback for
      #{@report.report_hash[:user][:name].truncate(25)}
    = link_to "Give Feedback", company_custom_assessment_user_user_assessment_report_url(:company_id => @report.report_hash[:company][:id], :user_id => @report.report_hash[:user][:id], :custom_assessment_id => @report.report_hash[:assessment][:id], :id => @report.id, :format => "html", :view_mode => "feedback", :host => Rails.application.config.action_mailer.default_url_options[:host]), :class => "give-feedback-btn btn btn-info"

    .clr
.divider3
