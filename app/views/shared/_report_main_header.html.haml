#header
  .container
    .logo
      - if @view_mode == "html"
        = link_to(image_tag("#{Rails.application.config.action_controller.asset_host}/#{asset_path("jombay-logo.png")}"), Rails.application.config.action_controller.asset_host)
      - else
        = link_to(wicked_pdf_image_tag("jombay-logo.png"), "/")

    .pull-right{style: "margin-top: 10px;"}
      - if @view_mode == "html"
        = link_to "View Printable PDF", company_assessment_candidate_candidate_assessment_report_url(:company_id => @report.report_hash[:company][:id], :candidate_id => @report.report_hash[:candidate][:id], :assessment_id => @report.report_hash[:assessment][:id], :id => @report.id, :format => "pdf", :host => Rails.application.config.action_mailer.default_url_options[:host]), :class => "btn btn-warning"
    .clr
