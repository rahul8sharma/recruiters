.test-flow-sub-nav.clearfix.pull-left
  .menu{ class: [:new, :norms, :styles].include?(params[:action].to_sym) ? "active" : "" }
    1 &nbsp; Create a Test 
  .menu{ class: [:add_candidates].include?(params[:action].to_sym) ? "active" : "" }
    2 &nbsp; Add Candidates
  .menu{ class: [:send_test_to_candidates, :send_test_to_employees].include?(params[:action].to_sym) ? "active" : "" }
    3 &nbsp; Confirm &amp; Send     
.clr
