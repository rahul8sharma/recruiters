%h1 All Items / Item Groups
- if can? :create, "ObjectiveItem"
  = link_to "Manage Objective Items", manage_objective_items_path

.pagination.pull-right
  = paginate @items
.clr
= render partial: 'index', locals: { items: @items }
.pagination.pull-right
  = paginate @items
.clr
