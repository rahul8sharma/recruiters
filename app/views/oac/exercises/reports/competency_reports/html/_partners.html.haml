.partners.text-center
  .container
    .divider2
    .big-text
      Partners
    .divider1
    .partner_logos
      - @report.report_data[:tools].each do |tool, data|
        - if data[:logo].present?
          = image_tag("report/html/oac/partner_logos/#{data[:logo]}")
    .divider1
