:ruby
  unless defined? options
    options = {}
  end

= content_for :stylesheets do
  = stylesheet_link_tag 'recruiters/landing'



= render :file => 'layouts/recruiters/base', :locals => {:options => options}
